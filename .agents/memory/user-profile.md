---
name: user-profile
description: "Who the user is, language preference, and the product they work on"
metadata: 
  node_type: memory
  type: user
  originSessionId: 8862bd9e-5d13-42a7-8b51-9079f8a21d37
  modified: 2026-08-27T10:03:00.879Z
---

Backend-leaning developer on **TA-ITS015**, an ITS (traffic-management) platform:
`TA-ITS015-WEBAPI-V1.0` (.NET 10 / ASP.NET Core / SqlSugar, modular monolith + worker services)
and `TA-ITS015-WEBVUE-V1.0` (Vue 3 / TS / Vite / Pinia). Both live as nested git repos under
`c:\ThienAn` (the parent repo tracks `.agents/`, docs, scripts).

Writes in **Vietnamese** and expects replies in Vietnamese. Comfortable with terse, technical
answers and tables. Email `hoangpham30113@gmail.com` (identity only).

**Ngôn ngữ báo cáo & tài liệu**: BẮT BUỘC dùng tiếng Việt trong sáng, tự nhiên, dễ hiểu cho người Việt đọc; TUYỆT ĐỐI HẠN CHẾ chêm các thuật ngữ tiếng Anh chuyên ngành khó hiểu (ví dụ: dùng **Mốc đánh dấu đã gửi / Gửi nối đuôi** thay vì *Watermark / Incremental*, dùng **Hồ sơ ánh xạ** thay vì *Mapping profile*, dùng **Bản chụp toàn bộ** thay vì *Snapshot*). Chỉ giữ tiếng Anh cho tên biến/cột CSDL hoặc chuẩn quốc tế bắt buộc (`LastTime`, `LastKey`, `HTTP`, `REST API`).

Domain: C2C / ISO 14827 (TCVN 13600) data sharing between traffic centers — packets 101–111,
partner mapping/codeset config, DATEX-style PDU envelopes.

**Cơ cấu nhân sự phân hệ ShareData & Dự án**:
- Phân hệ **ShareData**: Chỉ có **2 thành viên kỹ thuật duy nhất** là **Đạt** (Backend WebAPI, CSDL) và **Hiếu** (Ngô Văn Hiếu - Backend Worker, Data Engine, UI logic cấu hình). Tech Lead / Kiến trúc hệ thống là **Anh Sơn**.
- **Kiên**: **KHÔNG PHẢI FE** và **KHÔNG THUỘC PHÂN HỆ SHAREDATA**. Tuyệt đối không gán vai trò Frontend (FE) cho Kiên và không đưa Kiên vào phân hệ ShareData.
