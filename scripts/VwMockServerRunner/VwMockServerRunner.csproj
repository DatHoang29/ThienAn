<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>VwMockServerRunner</RootNamespace>
    <AssemblyName>VwMockServerRunner</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- Dùng lại nguyên file mock server trong tests/ qua Link thay vì ProjectReference tới
         test.csproj — test.csproj kéo theo toàn bộ TAC_WebAPI + tham chiếu DB, trong khi
         VwISAPIMockServerHikvision chỉ cần System.Net.HttpListener thuần. -->
    <Compile Include="..\..\tests\Modules\VideoWall\MockServer\VwISAPIMockServerHikvision.cs" Link="VwISAPIMockServerHikvision.cs" />
  </ItemGroup>

</Project>
