# _source — Bản gốc tài liệu (Human-only / Tier C)

> 👤 **AI không đọc thư mục này.** Toàn bộ nội dung cần thiết đã được chuyển thể sang các file Markdown trong thư mục `doc/`. Chỉ mở file trong thư mục này khi người dùng yêu cầu đích danh.
