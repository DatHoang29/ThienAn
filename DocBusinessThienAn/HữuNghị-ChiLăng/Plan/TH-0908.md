System + TMS

- Giao diện hiển thị -> Rà lại các phần hiển thị chưa hợp lý 
- Danh sách icon: Rà soát và hiển thị tất cả icon đã thực hiện để meeting đánh giá lần 2
- Notification


VMS
- Rà lại các chức năng và ràng buộc dữ liệu (kích thước khung hình, cảnh báo nhập liệu,...)
-> Tách ra dịch vụ để xử lý riêng lẻ

Credential Manager (user, pass, path, domain) -> he thong server se co domain

VDS
- Rà soát lại chức năng 
- Giao diện hiển thị + Dữ liệu
- Hiệu năng (Tài nguyên sử dụng, tốc độ xử lý)

- Lưu trữ hình ảnh (server xử lý hình ảnh Lưu/lấy hình)
Credential Manager (user, pass, path, domain) -> he thong server se co domain




CCTV
- Đăng nhập session/cookies tích hợp thiết bị (Xem xét mở proxy BE - Service)
- Băng thông sử dụng


FMS
- Chuyển đổi hệ thống tích hợp (Nagios -> Zabbix)
- Hiệu năng (tốc độ xử lý)
- Thông báo trạng thái thay đổi

VideoWall
- Tích hợp bộ điều khiển theo cấu trúc lắp đặt phân cấp (hình minh hoa trên group)
- Tích hợp với web

ShareData
- Luồng xử lý dữ liệu xoay vòng: Lấy dữ liệu -> Xử lý -> Gửi vào API -> API nhận -> xử lý -> Lưu trữ
- Lịch sử truyền nhận/ thiết lập
- Thiết lập dữ liệu / cấu trúc dữ liệu

- SSE + API/MQTT/.... rpc (1 endpoint để ttruy cap tat ca chuc nang)

WebPortal
- Server map offline build hoàn chỉnh 1 gói cài đặt để restore / tài liệu HD cài đặt/xử lý sự cố nếu có
- Dịch vụ đồng bộ dữ liệu (-> SSE + MessagePack)


Toll
- Dịch vụ đồng bộ dữ liệu
- Đối chiếu dữ liệu 2 bên



Hình ảnh/ FIle -> NAS / SAN 
Network share (\\...) -> OS (code c#) -> web tèo

Cluster web -> web/dịch vụ -> 2 web chạy song song/ dịch vụ (WPF/Worker -> Form/Service)

2 api luu file, doc file

api/get (ten file/ thu muc...)
File.Open(...) -> stream byte/ base64






 
